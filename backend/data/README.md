# Data Directory

This directory is created and used by the backend container.

- `raw/` - raw parser output from lalafo.kg
- `processed/` - cleaned datasets ready for the database
- `models/` - trained ML model files
